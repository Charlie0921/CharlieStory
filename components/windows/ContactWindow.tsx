import { PROFILE } from "@/lib/data";

export default function ContactWindow() {
  return <div className="mx-auto max-w-2xl"><div className="grid gap-5 sm:grid-cols-[1fr_190px]"><div><p className="eyebrow">Interoffice memo</p><h3 className="font-display text-3xl font-semibold text-ink">Let’s connect.</h3><p className="mt-3 text-sm leading-relaxed text-ink-soft">Email me about software engineering roles, business systems, research, or collaboration.</p><a href={`mailto:${PROFILE.email}?subject=Hello%20from%20your%20portfolio`} className="primary-button mt-5 inline-flex">Send Email →</a></div><div className="rotate-1 rounded-lg border border-line bg-sticky p-4 shadow-soft"><p className="font-mono text-[0.58rem] uppercase tracking-widest text-ink-soft">Pinned contact</p><p className="mt-3 break-all text-sm text-ink">{PROFILE.email}</p></div></div><div className="mt-7 grid gap-2 sm:grid-cols-3"><ContactLink href={`mailto:${PROFILE.email}`} label="EMAIL" value="Write a note" /><ContactLink href={PROFILE.linkedin} label="LINKEDIN" value="Connect" external /><ContactLink href={PROFILE.github} label="GITHUB" value="View code" external /></div></div>;
}
function ContactLink({ href, label, value, external }: { href: string; label: string; value: string; external?: boolean }) { return <a href={href} target={external ? "_blank" : undefined} rel={external ? "noreferrer" : undefined} className="rounded-lg border border-line bg-white p-4 transition hover:-translate-y-0.5 hover:border-ice-deep"><p className="eyebrow">{label}</p><p className="mt-1 text-sm text-ink">{value} ↗</p></a>; }
